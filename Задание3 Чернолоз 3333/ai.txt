Запрос был:
"В уже готовый Node.js сервер и клиентский JS (index.html + client.js) добавь функциональность редактирования:  
- Сервер: PUT /edit/:id, принимающий JSON { text }, обновляющий запись в MySQL.  
- Клиент: при нажатии Edit превращай <td> в <input> и рисуй кнопки Save/Cancel. Save делает PUT /edit/:id, затем заново GET /list, Cancel сбрасывает режим редактирования. "